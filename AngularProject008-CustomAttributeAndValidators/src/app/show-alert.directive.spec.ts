import { ShowAlertDirective } from './show-alert.directive';

describe('ShowAlertDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowAlertDirective();
    expect(directive).toBeTruthy();
  });
});
